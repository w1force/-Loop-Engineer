# skill 能力 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 agent 加上 Claude Code 式 skill 能力——目录化 skill 包(`SKILL.md` + 只读资源),模型自主决策,经 `load_skill` 工具按需加载 skill 指令。

**Architecture:** 新增 `core/skills/` 包(loader 发现元数据 + load_skill 工具 + prepare_skills 注入),`agent_loop.submit` 调 `prepare_skills` 把 skill 目录拼到 `system` + `load_skill` 追加到 `tools`。`query_loop` / `tool_executor` / recovery **零改动**。`load_skill` 是普通 `Tool`(和 builtin_tools 同级,复用并发/权限/transcript/Tombstone)。

**Tech Stack:** Python 3.10+、pydantic v2、`pyyaml`(新增,frontmatter 解析)、pytest(asyncio_mode=auto)、pathlib 标准库。

## Global Constraints

(每个 task 隐式包含,值照 spec 抄)

- pytest `asyncio_mode = auto`:测试直接 `async def test_xxx()`,不加 `@pytest.mark.asyncio`;`tmp_path` 造临时 skill 目录。
- pyright `typeCheckingMode = "basic"` 必须通过。
- **新增依赖 pyyaml>=6**(frontmatter 解析;写入 `pyproject.toml` 的 `dependencies`)。
- 留在 `feat/skill` 分支,不提交 main;每 task 末尾 commit。
- **skill 是增强,失败降级**:任何 skill 层面失败 → 无注入 + warn,绝不中断 `query_loop`;`load_skill` 错误以文本返回(走 tool_result),**不抛异常**。
- frontmatter **宽松解析**:容忍未知字段(忽略),YAML 损坏/缺 `description` → 跳过该 skill。
- 目录名 = skill 标识(不引入 frontmatter.name)。
- 中文注释(对齐现有代码风格)。
- 现有测试模式参照 `tests/test_builtin_tools/test_read.py`:`_ctx() = ToolContext(tracer=NoopTracer(), abort_signal=asyncio.Event())`,工具调用 `await tool.func(InModel(...), _ctx())`。

## File Structure

| 文件 | 责任 | 动作 |
|------|------|------|
| `core/skills/loader.py` | `SkillMeta` + `SkillLoader.scan` + `render_catalog` + `append_catalog` | 新建 |
| `core/skills/load_skill.py` | `LoadSkillInput` + `load_skill_tool(metas)` + `prepare_skills(...)` | 新建 |
| `core/skills/__init__.py` | 包导出 | 新建 |
| `core/agent_loop.py` | `AgentConfig.skill_dirs` + `submit` 调 `prepare_skills` | 改 |
| `pyproject.toml` | 加 `pyyaml>=6` | 改 |
| `skills/bullet-summarize/SKILL.md` | 示范 skill | 新建 |
| `tests/test_skill_loader.py` | loader 单元测试(Task 1-2) | 新建 |
| `tests/test_load_skill.py` | load_skill 工具测试(Task 3) | 新建 |
| `tests/test_agent_loop_skill.py` | prepare_skills 注入测试(Task 4) | 新建 |
| `tests/test_skill_smoke.py` | 示范 skill 冒烟(Task 5) | 新建 |

> 注:`prepare_skills` 放 `load_skill.py`(不放 `loader.py`)——因为 `prepare_skills` 调用 `load_skill_tool`,放同文件避免 `loader → load_skill → loader` 循环导入;`load_skill.py` 单向 import `loader`。

---

### Task 1: skill 发现 + frontmatter 解析(`SkillMeta` + `SkillLoader.scan`)

**Files:**
- Create: `core/skills/__init__.py`
- Create: `core/skills/loader.py`
- Create: `tests/test_skill_loader.py`
- Modify: `pyproject.toml`(dependencies 加 `pyyaml>=6`)

**Interfaces:**
- Consumes: 无(首个 task)
- Produces:
  - `SkillMeta` — frozen dataclass:`name: str`、`description: str`、`skill_dir: Path`、`skill_md: Path`
  - `SkillLoader.scan(skill_dirs: list[str | Path]) -> list[SkillMeta]` — 静态方法,按 `name` 排序,空输入/路径不存在返回 `[]`

- [ ] **Step 1: 加 pyyaml 依赖**

修改 `pyproject.toml` 的 `dependencies` 段,追加一行:
```toml
[project]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2",
    "pydantic-settings>=2",
    "aiofiles>=23",
    "pyyaml>=6",
]
```

然后安装:
```bash
uv sync
```

- [ ] **Step 2: 写失败测试** `tests/test_skill_loader.py`

```python
"""Task 1: SkillLoader.scan + frontmatter 解析测试。"""
from pathlib import Path

from core.skills import SkillLoader, SkillMeta


def _make_skill(root: Path, name: str, body: str) -> Path:
    """在 root 下造一个 skill 目录 + SKILL.md。"""
    d = root / name
    d.mkdir(parents=True)
    (d / "SKILL.md").write_text(body, encoding="utf-8")
    return d


def test_scan_basic_sorted(tmp_path):
    skills = tmp_path / "skills"
    _make_skill(skills, "foo", "---\ndescription: foo skill\n---\n# foo\n")
    _make_skill(skills, "bar", "---\ndescription: bar skill\n---\n# bar\n")
    metas = SkillLoader.scan([skills])
    assert [m.name for m in metas] == ["bar", "foo"]  # 按 name 排序


def test_scan_missing_description_skipped(tmp_path):
    skills = tmp_path / "skills"
    _make_skill(skills, "foo", "---\nname: foo\n---\n# foo\n")  # 无 description
    assert SkillLoader.scan([skills]) == []


def test_scan_yaml_corrupt_skipped(tmp_path):
    skills = tmp_path / "skills"
    _make_skill(skills, "foo", "---\ndescription: [unclosed\n---\n# foo\n")
    assert SkillLoader.scan([skills]) == []


def test_scan_unknown_field_tolerated(tmp_path):
    skills = tmp_path / "skills"
    body = "---\ndescription: foo\nwhen_to_use: bar\nallowed-tools: read\n---\n# foo\n"
    _make_skill(skills, "foo", body)
    metas = SkillLoader.scan([skills])
    assert len(metas) == 1 and metas[0].name == "foo"


def test_scan_nonexistent_dir_skipped(tmp_path):
    assert SkillLoader.scan([tmp_path / "nope"]) == []


def test_scan_duplicate_name_later_overrides(tmp_path):
    a = tmp_path / "a"; b = tmp_path / "b"
    _make_skill(a, "foo", "---\ndescription: from-a\n---\n")
    _make_skill(b, "foo", "---\ndescription: from-b\n---\n")
    metas = SkillLoader.scan([a, b])
    assert len(metas) == 1 and metas[0].description == "from-b"


def test_scan_no_skill_md_skipped(tmp_path):
    skills = tmp_path / "skills"
    (skills / "notaskill").mkdir(parents=True)
    assert SkillLoader.scan([skills]) == []


def test_scan_empty_dirs():
    assert SkillLoader.scan([]) == []


def test_skill_meta_fields(tmp_path):
    skills = tmp_path / "skills"
    _make_skill(skills, "foo", "---\ndescription: foo skill\n---\n# foo\n")
    m = SkillLoader.scan([skills])[0]
    assert isinstance(m, SkillMeta)
    assert m.name == "foo"
    assert m.description == "foo skill"
    assert m.skill_md == skills / "foo" / "SKILL.md"
    assert m.skill_dir == skills / "foo"


def test_scan_multiline_description(tmp_path):
    skills = tmp_path / "skills"
    body = "---\ndescription: |\n  line one\n  line two\n---\n# foo\n"
    _make_skill(skills, "foo", body)
    desc = SkillLoader.scan([skills])[0].description
    assert "line one" in desc and "line two" in desc
```

- [ ] **Step 3: 验证测试失败**

```bash
uv run pytest tests/test_skill_loader.py -v
```
Expected: FAIL(ImportError: cannot import `SkillLoader`/`SkillMeta` from `core.skills`)。

- [ ] **Step 4: 实现** `core/skills/loader.py`

```python
"""skill 发现与元数据解析。

扫描 skill_dirs 的直接子目录,解析每个 SKILL.md 的 YAML frontmatter,产出 SkillMeta。
frontmatter 宽松:容忍未知字段,YAML 损坏/缺 description → 跳过该 skill(不中断整体扫描)。
"""
from __future__ import annotations

import logging
from dataclasses import dataclass
from pathlib import Path

import yaml

logger = logging.getLogger(__name__)


@dataclass(frozen=True)
class SkillMeta:
    """一个 skill 的元数据。"""
    name: str            # = 目录名,skill 标识(load_skill 入参)
    description: str     # frontmatter.description,进 system 目录段
    skill_dir: Path      # skill 目录绝对路径
    skill_md: Path       # SKILL.md 绝对路径(= skill_dir / "SKILL.md")


def _parse_frontmatter(skill_md: Path) -> dict:
    """解析 SKILL.md 的 YAML frontmatter。
    无 frontmatter / 无闭合 --- / YAML 损坏 / 非 dict → 返回 {}。"""
    text = skill_md.read_text(encoding="utf-8")
    if not text.startswith("---"):
        return {}
    lines = text.splitlines()
    end = None
    for i in range(1, len(lines)):
        if lines[i].strip() == "---":
            end = i
            break
    if end is None:
        return {}
    fm_text = "\n".join(lines[1:end])
    try:
        meta = yaml.safe_load(fm_text)
    except yaml.YAMLError:
        return {}
    return meta if isinstance(meta, dict) else {}


class SkillLoader:
    @staticmethod
    def scan(skill_dirs: list[str | Path]) -> list[SkillMeta]:
        """扫描所有 skill_dirs 的直接子目录,解析 SKILL.md frontmatter。
        返回按 name 排序的 list[SkillMeta]。容错:单 skill 失败不影响其他。"""
        metas: dict[str, SkillMeta] = {}  # name -> meta(同 name 后者覆盖)
        for d in skill_dirs:
            root = Path(d)
            if not root.is_dir():
                logger.warning("skill dir not found, skipped: %s", root)
                continue
            for child in sorted(root.iterdir()):
                if not child.is_dir():
                    continue
                skill_md = child / "SKILL.md"
                if not skill_md.is_file():
                    continue  # 无 SKILL.md:当普通子目录,静默跳过
                fm = _parse_frontmatter(skill_md)
                desc = fm.get("description")
                if not isinstance(desc, str) or not desc.strip():
                    logger.warning("skill missing description, skipped: %s", child.name)
                    continue
                if child.name in metas:
                    logger.warning("duplicate skill name, later overrides: %s", child.name)
                metas[child.name] = SkillMeta(
                    name=child.name,
                    description=desc.strip(),
                    skill_dir=child,
                    skill_md=skill_md,
                )
        return sorted(metas.values(), key=lambda m: m.name)
```

- [ ] **Step 5: 实现** `core/skills/__init__.py`

```python
"""skill 系统:发现 + 元数据 + 目录注入 + load_skill 工具。"""
from .loader import SkillLoader, SkillMeta

__all__ = ["SkillLoader", "SkillMeta"]
```

- [ ] **Step 6: 验证测试通过**

```bash
uv run pytest tests/test_skill_loader.py -v
```
Expected: 10 passed。

- [ ] **Step 7: pyright 检查**

```bash
uv run pyright core/skills/loader.py core/skills/__init__.py tests/test_skill_loader.py
```
Expected: 0 errors。

- [ ] **Step 8: Commit**

```bash
git add pyproject.toml uv.lock core/skills/__init__.py core/skills/loader.py tests/test_skill_loader.py
git commit -m "feat: skill 发现与 frontmatter 解析(SkillMeta + SkillLoader.scan + pyyaml)"
```

---

### Task 2: 目录渲染(`render_catalog` + `append_catalog`)

**Files:**
- Modify: `core/skills/loader.py`(追加两个函数)
- Modify: `core/skills/__init__.py`(导出)
- Modify: `tests/test_skill_loader.py`(追加测试)

**Interfaces:**
- Consumes: `SkillMeta`(Task 1)
- Produces:
  - `render_catalog(metas: list[SkillMeta]) -> str` — 空 metas 返回 `""`;否则返回 `<skills>...</skills>` + 调用指引
  - `append_catalog(system: str | list[dict], catalog: str) -> str | list[dict]` — str 末尾拼接 / list 追加 `{"type":"text","text":catalog}`

- [ ] **Step 1: 写失败测试**(追加到 `tests/test_skill_loader.py` 末尾)

```python
from core.skills import render_catalog, append_catalog


def test_render_catalog_format(tmp_path):
    skills = tmp_path / "skills"
    _make_skill(skills, "foo", "---\ndescription: foo skill\n---\n")
    out = render_catalog(SkillLoader.scan([skills]))
    assert "<skills>" in out and "</skills>" in out
    assert "name: foo" in out
    assert "foo skill" in out
    assert "load_skill(name)" in out


def test_render_catalog_empty():
    assert render_catalog([]) == ""


def test_render_catalog_multiline_description(tmp_path):
    skills = tmp_path / "skills"
    _make_skill(skills, "foo", "---\ndescription: |\n  line one\n  line two\n---\n")
    out = render_catalog(SkillLoader.scan([skills]))
    assert "line one" in out and "line two" in out


def test_append_catalog_str():
    assert append_catalog("abc", "X") == "abcX"


def test_append_catalog_list():
    base = [{"type": "text", "text": "a"}]
    out = append_catalog(base, "X")
    assert isinstance(out, list)
    assert out[0] == {"type": "text", "text": "a"}
    assert out[1] == {"type": "text", "text": "X"}
```

- [ ] **Step 2: 验证测试失败**

```bash
uv run pytest tests/test_skill_loader.py -v
```
Expected: 新 5 个测试 FAIL(cannot import `render_catalog`/`append_catalog`)。

- [ ] **Step 3: 实现**(追加到 `core/skills/loader.py` 末尾)

```python
def render_catalog(metas: list[SkillMeta]) -> str:
    """metas → <skills>...</skills> 目录段 + 调用指引。空 metas 返回 ''。"""
    if not metas:
        return ""
    lines = ["", "", "<skills>"]
    for m in metas:
        desc = " ".join(m.description.split())  # 压缩多行空白成单行
        lines.append(f"- name: {m.name}")
        lines.append(f"  description: {desc}")
    lines.append("</skills>")
    lines.append("")
    lines.append("当用户请求匹配某个 skill 时,调用 load_skill(name) 加载完整指令后再执行。")
    return "\n".join(lines)


def append_catalog(system: str | list[dict], catalog: str) -> str | list[dict]:
    """把 catalog 拼到 system 末尾,兼容 str 与 list[dict] 两种形态。"""
    if isinstance(system, str):
        return system + catalog
    return [*system, {"type": "text", "text": catalog}]
```

- [ ] **Step 4: 更新导出** `core/skills/__init__.py`

```python
"""skill 系统:发现 + 元数据 + 目录注入 + load_skill 工具。"""
from .loader import SkillLoader, SkillMeta, render_catalog, append_catalog

__all__ = ["SkillLoader", "SkillMeta", "render_catalog", "append_catalog"]
```

- [ ] **Step 5: 验证测试通过**

```bash
uv run pytest tests/test_skill_loader.py -v
```
Expected: 15 passed(原 10 + 新 5)。

- [ ] **Step 6: pyright 检查**

```bash
uv run pyright core/skills/loader.py core/skills/__init__.py tests/test_skill_loader.py
```
Expected: 0 errors。

- [ ] **Step 7: Commit**

```bash
git add core/skills/loader.py core/skills/__init__.py tests/test_skill_loader.py
git commit -m "feat: skill 目录渲染(render_catalog + append_catalog,str|list 适配)"
```

---

### Task 3: `load_skill` 工具

**Files:**
- Create: `core/skills/load_skill.py`
- Modify: `core/skills/__init__.py`(导出)
- Create: `tests/test_load_skill.py`

**Interfaces:**
- Consumes: `SkillMeta`(Task 1)、`Tool` / `ToolContext`(现有 `core.tools`)
- Produces:
  - `LoadSkillInput(BaseModel)` — `name: str`
  - `load_skill_tool(metas: list[SkillMeta]) -> Tool` — 工厂闭包,`is_concurrency_safe=True`,`func(inp, ctx) -> str`

- [ ] **Step 1: 写失败测试** `tests/test_load_skill.py`

```python
"""Task 3: load_skill 工具测试。"""
import asyncio

from core.skills import SkillLoader, load_skill_tool, LoadSkillInput
from core.tools import Tool, ToolContext
from telemetry.tracer import NoopTracer


def _ctx() -> ToolContext:
    return ToolContext(tracer=NoopTracer(), abort_signal=asyncio.Event())


def _make_skill(root, name, body):
    d = root / name; d.mkdir(parents=True)
    (d / "SKILL.md").write_text(body, encoding="utf-8")


async def test_load_skill_returns_full_text(tmp_path):
    _make_skill(tmp_path, "foo", "---\ndescription: foo\n---\n# body\ninstructions\n")
    tool = load_skill_tool(SkillLoader.scan([tmp_path]))
    result = await tool.func(LoadSkillInput(name="foo"), _ctx())
    assert isinstance(result, str)
    assert "# body" in result
    assert "instructions" in result
    assert "description: foo" in result  # 全文含 frontmatter


async def test_load_skill_not_found(tmp_path):
    _make_skill(tmp_path, "foo", "---\ndescription: foo\n---\n# foo\n")
    tool = load_skill_tool(SkillLoader.scan([tmp_path]))
    result = await tool.func(LoadSkillInput(name="bar"), _ctx())
    assert isinstance(result, str)
    assert "not found" in result.lower()
    assert "foo" in result  # Available 列表含可用 skill


async def test_load_skill_read_failure(tmp_path):
    _make_skill(tmp_path, "foo", "---\ndescription: foo\n---\n# foo\n")
    tool = load_skill_tool(SkillLoader.scan([tmp_path]))
    (tmp_path / "foo" / "SKILL.md").unlink()  # 模拟读失败
    result = await tool.func(LoadSkillInput(name="foo"), _ctx())
    assert isinstance(result, str)
    assert "error" in result.lower()


def test_load_skill_is_concurrency_safe(tmp_path):
    _make_skill(tmp_path, "foo", "---\ndescription: foo\n---\n# foo\n")
    tool = load_skill_tool(SkillLoader.scan([tmp_path]))
    assert isinstance(tool, Tool)
    assert tool.is_concurrency_safe is True
    assert tool.name == "load_skill"
```

- [ ] **Step 2: 验证测试失败**

```bash
uv run pytest tests/test_load_skill.py -v
```
Expected: FAIL(cannot import `load_skill_tool`/`LoadSkillInput`)。

- [ ] **Step 3: 实现** `core/skills/load_skill.py`

```python
"""load_skill 工具:按需加载指定 skill 的 SKILL.md 全文(对标 CC progressive disclosure)。

模型先看 system 里的 <skills> 目录(name+description),决策后调用本工具加载完整指令。
工厂闭包捕获 metas,和 builtin_tools(read_state) 同模式。
"""
from __future__ import annotations

from pydantic import BaseModel

from core.tools import Tool, ToolContext
from .loader import SkillMeta


class LoadSkillInput(BaseModel):
    name: str


def load_skill_tool(metas: list[SkillMeta]) -> Tool:
    """工厂闭包:捕获 metas,返回 load_skill Tool。"""
    index = {m.name: m for m in metas}

    async def _load(inp: LoadSkillInput, ctx: ToolContext) -> str:
        meta = index.get(inp.name)
        if meta is None:
            return f"Error: skill '{inp.name}' not found. Available: {sorted(index)}"
        try:
            return meta.skill_md.read_text(encoding="utf-8")  # 返回 SKILL.md 全文
        except OSError as e:
            return f"Error: cannot read skill '{inp.name}': {e}"

    return Tool(
        name="load_skill",
        description="加载指定 skill 的完整指令。先看 <skills> 目录决定用哪个 skill,再调用此工具。",
        input_model=LoadSkillInput,
        func=_load,
        is_concurrency_safe=True,  # 只读 → 可并发
    )
```

- [ ] **Step 4: 更新导出** `core/skills/__init__.py`

```python
"""skill 系统:发现 + 元数据 + 目录注入 + load_skill 工具。"""
from .loader import SkillLoader, SkillMeta, render_catalog, append_catalog
from .load_skill import load_skill_tool, LoadSkillInput

__all__ = [
    "SkillLoader",
    "SkillMeta",
    "render_catalog",
    "append_catalog",
    "load_skill_tool",
    "LoadSkillInput",
]
```

- [ ] **Step 5: 验证测试通过**

```bash
uv run pytest tests/test_load_skill.py -v
```
Expected: 4 passed。

- [ ] **Step 6: pyright 检查**

```bash
uv run pyright core/skills/load_skill.py core/skills/__init__.py tests/test_load_skill.py
```
Expected: 0 errors。

- [ ] **Step 7: Commit**

```bash
git add core/skills/load_skill.py core/skills/__init__.py tests/test_load_skill.py
git commit -m "feat: load_skill 工具(按需加载 SKILL.md 全文,只读可并发)"
```

---

### Task 4: agent_loop 集成(`prepare_skills` + `AgentConfig.skill_dirs`)

**Files:**
- Modify: `core/skills/load_skill.py`(追加 `prepare_skills`)
- Modify: `core/skills/__init__.py`(导出 `prepare_skills`)
- Modify: `core/agent_loop.py`(`AgentConfig.skill_dirs` + `submit` 调用)
- Create: `tests/test_agent_loop_skill.py`

**Interfaces:**
- Consumes: `SkillLoader.scan`、`render_catalog`、`append_catalog`、`load_skill_tool`(Task 1-3)
- Produces:
  - `prepare_skills(skill_dirs, system, tools) -> tuple[str | list[dict], list[Tool]]` — 扫描+注入;无 skill / 异常 → 原样返回(降级)
  - `AgentConfig.skill_dirs: list[str]`(默认 `["skills/"]`)
  - `submit` 接线:`system, tools = prepare_skills(config.skill_dirs, config.system, tools)`

- [ ] **Step 1: 写失败测试** `tests/test_agent_loop_skill.py`

```python
"""Task 4: skill 注入逻辑(prepare_skills)测试。

prepare_skills 是纯函数,覆盖注入/降级/拼接;submit 接线靠 pyright + Task 5 冒烟。
"""
from pydantic import BaseModel

from core.skills import prepare_skills
from core.tools import Tool


class _DummyIn(BaseModel):
    pass


def _dummy_tool(name: str) -> Tool:
    async def _f(inp, ctx):
        return "ok"
    return Tool(name=name, description="d", input_model=_DummyIn, func=_f)


def test_prepare_skills_injects(tmp_path):
    skills = tmp_path / "skills"
    d = skills / "foo"; d.mkdir(parents=True)
    (d / "SKILL.md").write_text("---\ndescription: foo\n---\n# foo\n", encoding="utf-8")
    existing = [_dummy_tool("read")]
    system, tools = prepare_skills([skills], "base system", existing)
    assert "<skills>" in system
    assert "foo" in system
    assert any(t.name == "load_skill" for t in tools)
    assert any(t.name == "read" for t in tools)  # 原工具保留


def test_prepare_skills_empty_dirs_noop():
    existing = [_dummy_tool("read")]
    system, tools = prepare_skills([], "base", existing)
    assert system == "base"
    assert tools == existing


def test_prepare_skills_nonexistent_dir_noop(tmp_path):
    existing = [_dummy_tool("read")]
    system, tools = prepare_skills([tmp_path / "nope"], "base", existing)
    assert system == "base"
    assert tools == existing


def test_prepare_skills_str_system(tmp_path):
    skills = tmp_path / "skills"
    d = skills / "foo"; d.mkdir(parents=True)
    (d / "SKILL.md").write_text("---\ndescription: foo\n---\n# foo\n", encoding="utf-8")
    system, _ = prepare_skills([skills], "abc", [])
    assert isinstance(system, str)
    assert system.startswith("abc")


def test_prepare_skills_list_system(tmp_path):
    skills = tmp_path / "skills"
    d = skills / "foo"; d.mkdir(parents=True)
    (d / "SKILL.md").write_text("---\ndescription: foo\n---\n# foo\n", encoding="utf-8")
    base = [{"type": "text", "text": "a"}]
    system, _ = prepare_skills([skills], base, [])
    assert isinstance(system, list)
    assert system[0] == {"type": "text", "text": "a"}
    assert system[-1]["type"] == "text"
    assert "<skills>" in system[-1]["text"]


def test_prepare_skills_scan_exception_degrades(monkeypatch, tmp_path):
    from core.skills import loader as loader_mod

    def _boom(_dirs):
        raise RuntimeError("boom")

    monkeypatch.setattr(loader_mod.SkillLoader, "scan", _boom)
    existing = [_dummy_tool("read")]
    system, tools = prepare_skills([tmp_path], "base", existing)
    assert system == "base"  # 降级:不注入
    assert tools == existing
```

- [ ] **Step 2: 验证测试失败**

```bash
uv run pytest tests/test_agent_loop_skill.py -v
```
Expected: FAIL(cannot import `prepare_skills`)。

- [ ] **Step 3: 实现 `prepare_skills`**(追加到 `core/skills/load_skill.py`)

Task 3 的 `load_skill.py` 已 import `Tool`/`ToolContext`/`BaseModel`/`SkillMeta`。这里把顶部 import 段整理为(补 `logging`/`Path`/`SkillLoader`/`render_catalog`/`append_catalog` + 模块级 `logger`):
```python
import logging
from pathlib import Path

from pydantic import BaseModel

from core.tools import Tool, ToolContext
from .loader import SkillMeta, SkillLoader, render_catalog, append_catalog

logger = logging.getLogger(__name__)
```

在文件末尾追加:
```python
def prepare_skills(
    skill_dirs: list[str | Path],
    system: str | list[dict],
    tools: list[Tool],
) -> tuple[str | list[dict], list[Tool]]:
    """扫描 + 注入:有 skill 则拼目录到 system + 追加 load_skill 工具。
    无 skill / 扫描异常 → 原样返回(降级,不中断主流程)。"""
    try:
        metas = SkillLoader.scan(skill_dirs)
    except Exception as e:
        logger.warning("skill scan failed, disabled: %s", e)
        return system, tools
    if not metas:
        return system, tools
    system = append_catalog(system, render_catalog(metas))
    tools = [*tools, load_skill_tool(metas)]
    return system, tools
```

- [ ] **Step 4: 更新导出** `core/skills/__init__.py`

```python
"""skill 系统:发现 + 元数据 + 目录注入 + load_skill 工具。"""
from .loader import SkillLoader, SkillMeta, render_catalog, append_catalog
from .load_skill import load_skill_tool, LoadSkillInput, prepare_skills

__all__ = [
    "SkillLoader",
    "SkillMeta",
    "render_catalog",
    "append_catalog",
    "load_skill_tool",
    "LoadSkillInput",
    "prepare_skills",
]
```

- [ ] **Step 5: 改 `AgentConfig`**(`core/agent_loop.py`)

在 `AgentConfig` dataclass 末尾(`tool_execution_mode` 之后)加字段:
```python
    tool_execution_mode: Literal["streaming", "batch"] = "streaming"
    skill_dirs: list[str] = field(default_factory=lambda: ["skills/"])
```

- [ ] **Step 6: 改 `submit`**(`core/agent_loop.py`)

顶部 import 段加:
```python
from .skills import prepare_skills
```

在 `submit` 内,`tools = [*config.tools, *builtin_tools(read_state)]` 之后、`params = QueryParams(...)` 之前插入:
```python
    read_state = FileReadState()
    tools = [*config.tools, *builtin_tools(read_state)]

    system, tools = prepare_skills(config.skill_dirs, config.system, tools)  # ★ skill 注入

    params = QueryParams(
        messages=messages,
        system=system,  # ← 原 config.system 改为 system(可能已拼 skill 目录)
        model=config.model,
        max_tokens=config.max_tokens,
        provider=config.provider,
        abort_signal=config.abort_signal,
        tools=tools,
        max_turns=config.max_turns,
        can_use_tool=config.can_use_tool,
        tool_execution_mode=config.tool_execution_mode,
    )
```

- [ ] **Step 7: 验证测试通过**

```bash
uv run pytest tests/test_agent_loop_skill.py -v
```
Expected: 6 passed。

- [ ] **Step 8: pyright 检查**

```bash
uv run pyright core/skills/load_skill.py core/skills/__init__.py core/agent_loop.py tests/test_agent_loop_skill.py
```
Expected: 0 errors。

- [ ] **Step 9: 全量回归**

```bash
uv run pytest -v
```
Expected: 全绿(原 148 + 本 feature 新增,无回归)。

- [ ] **Step 10: Commit**

```bash
git add core/skills/load_skill.py core/skills/__init__.py core/agent_loop.py tests/test_agent_loop_skill.py
git commit -m "feat: agent_loop skill 集成(prepare_skills 注入 + 降级 + AgentConfig.skill_dirs)"
```

---

### Task 5: 示范 skill `bullet-summarize` + 冒烟

**Files:**
- Create: `skills/bullet-summarize/SKILL.md`
- Create: `tests/test_skill_smoke.py`

**Interfaces:**
- Consumes: `SkillLoader.scan`(Task 1)
- Produces: 真实示范 skill `skills/bullet-summarize/`(验证完整发现链路)

- [ ] **Step 1: 写失败测试** `tests/test_skill_smoke.py`

```python
"""Task 5: 示范 skill bullet-summarize 冒烟测试(扫描真实 skills/)。"""
from pathlib import Path

from core.skills import SkillLoader

_PROJECT_ROOT = Path(__file__).resolve().parents[1]
_SKILLS_DIR = _PROJECT_ROOT / "skills"


def test_bullet_summarize_discoverable():
    """skills/bullet-summarize 能被 scan 发现。"""
    metas = SkillLoader.scan([_SKILLS_DIR])
    assert "bullet-summarize" in [m.name for m in metas]


def test_bullet_summarize_description_present():
    """bullet-summarize 的 description 非空且语义正确。"""
    metas = SkillLoader.scan([_SKILLS_DIR])
    m = next((x for x in metas if x.name == "bullet-summarize"), None)
    assert m is not None
    assert "总结" in m.description or "summarize" in m.description.lower()
```

- [ ] **Step 2: 验证测试失败**

```bash
uv run pytest tests/test_skill_smoke.py -v
```
Expected: FAIL(`bullet-summarize` 不在发现列表,因 `skills/` 目录还不存在)。

- [ ] **Step 3: 创建示范 skill** `skills/bullet-summarize/SKILL.md`

```markdown
---
description: |
  当用户要求总结文本、文章或对话要点时使用。
  输出 3-5 个简洁 bullet 要点,每个不超过 20 字。
---

# bullet-summarize

总结时遵循:
1. 提取 3-5 个核心要点
2. 每个要点以 "- " 开头,不超过 20 字
3. 按重要性排序
4. 末尾不加客套话
```

- [ ] **Step 4: 验证测试通过**

```bash
uv run pytest tests/test_skill_smoke.py -v
```
Expected: 2 passed。

- [ ] **Step 5: 全量回归**

```bash
uv run pytest -v
```
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add skills/bullet-summarize/SKILL.md tests/test_skill_smoke.py
git commit -m "feat: 示范 skill bullet-summarize + 冒烟测试"
```

---

## Self-Review(plan 写完后自查)

**1. Spec 覆盖:**
- 发现 + frontmatter 解析 → Task 1 ✓
- 目录注入(render_catalog + append_catalog) → Task 2 ✓
- load_skill 工具 → Task 3 ✓
- agent_loop 集成(AgentConfig.skill_dirs + submit) → Task 4 ✓
- 错误处理矩阵(降级/容错) → Task 1 scan 容错 + Task 3 load_skill Error 文本 + Task 4 prepare_skills 降级 ✓
- 测试策略(单元/集成/冒烟) → Task 1-5 ✓
- 示范 skill bullet-summarize → Task 5 ✓
- pyyaml 依赖 → Task 1 Step 1 ✓
- 不加 enable_skills 开关 → Task 4(仅 skill_dirs) ✓

**2. 占位符扫描:** 无 TBD/TODO,每步含完整代码 ✓

**3. 类型一致性:**
- `SkillMeta` 字段(name/description/skill_dir/skill_md)跨 Task 1-5 一致 ✓
- `SkillLoader.scan(skill_dirs) -> list[SkillMeta]` 一致 ✓
- `render_catalog(metas) -> str` / `append_catalog(system, catalog) -> str|list[dict]` 一致 ✓
- `load_skill_tool(metas) -> Tool` / `LoadSkillInput.name` 一致 ✓
- `prepare_skills(skill_dirs, system, tools) -> tuple[system, tools]` 一致 ✓
